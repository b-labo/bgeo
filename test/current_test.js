/**
 * Test case for current.
 * Runs with mocha.
 */
'use strict'

const current = require('../lib/current.js')
const assert = require('assert')
const co = require('co')

describe('current', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Current', () => co(function * () {

  }))
})

/* global describe, before, after, it */
